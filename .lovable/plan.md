Add a "See my work" button beneath the About paragraphs in `src/pages/Index.tsx` (inside the `#about` section, after line 184). It links to `/work` (the portfolio page) using a React Router `<Link>`, styled to match the existing site CTAs (subtle bordered/pill button on the dark background). No other changes.